#include <iostream>
#include <set>
#include <unordered_map>
#include <vector>
#include <algorithm>
#include <math.h>
using namespace std;

int main()
{
    int N;
    cin >> N;

    int ages[N][N];
    for (int i = 0; i < N; ++i) {
        for (int j = 0; j < N; ++j)
            cin >> ages[i][j];
    }

    //Rows
    for (int i = 0; i < N; ++i) {
    }
}